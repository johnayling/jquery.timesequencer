(function ( $ ) {
    $.fn.timesequence = function( options ) {

        var settings = $.extend({
            // These are the defaults.
            length: 60 //seconds

        }, options );

        var container = this;








    };
}( jQuery ));